#include "main.h"
/**
 *
 * main - entry point
 *
 * Return: returns zero
 */
void print_alphabet(void)
{
	char alphabet;
	
	for (alphabe = 'a'; alphabet <= 'z'; alphabet++)
	{
		_putchar(alphabet);
	}

	_putchar(alphabet);

}

