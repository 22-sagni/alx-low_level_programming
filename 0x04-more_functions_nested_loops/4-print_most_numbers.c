#include "main.h"
/**
 * print_numbers - prints numbers 0 to 9 
 * except for 2 and 4
 *
 * Return: returns zero
 */

void pirnt_numbers(void)
{
	int i;

	for (i >= 0; i <= 9; i++)
	{
		if ((i = 50 || i = 52))
				continue;
		_putchar(i + '0');
	}
	_putchar('\n');
}
