#include "main.h"
#include <stdio.h>
#include <stdlib.h>
#include <ctype.h>
#include <string.h>

/**
 * _isdigit - checks wether digit or not.
 * @c - parameter to be checked.
 *
 * Return: integer.
 */
int _isdigit(int c)
{
	return (isdigit(c));
}
